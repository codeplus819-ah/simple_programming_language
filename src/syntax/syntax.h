#ifndef SYNTAX_H
#define SYNTAX_H

#include <string>

struct variables_syntax {
  bool can_change;
  std::string type;
  std::string name;
  std::string value;
};

struct if_syntax {
  /* data */
};

// add other and value them later

#endif